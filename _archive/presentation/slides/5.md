---
layout: default
---
<LogoBar variant="black" position="header" align="right" />

&nbsp;

# Código

<!-- Círculo de Destaque (click-2) -->
<div 
  class="absolute border-3 border-red-500 rounded-full w-12 h-5 pointer-events-none z-50 transition-all duration-300"
  :class="$clicks === 2
  ? 'opacity-100 scale-100 delay-500'  
  : 'opacity-0 !important scale-0 pointer-events-none delay-0'"
  style="top: 192px; left: 212px;"
>
</div>

<!-- Círculo de Destaque (click-4) -->
<div 
  class="absolute border-3 border-red-500 rounded-full w-22 h-5 pointer-events-none z-50 transition-all duration-300"
  :class="$clicks === 4
  ? 'opacity-100 scale-100 delay-0' 
  : 'opacity-0 !important scale-0 pointer-events-none delay-0'"
  style="top: 242px; left: 306px;"
>
</div>

<div 
  class="absolute border-3 border-red-500 rounded-full w-82 h-5 pointer-events-none z-50 transition-all duration-300"
  :class="$clicks === 4
  ? 'opacity-100 scale-100 delay-0' 
  : 'opacity-0 !important scale-0 pointer-events-none delay-0'"
  style="top: 364px; left: 274px;"
>
</div>

<!-- Círculo de Destaque (click-6) -->
<div 
  class="absolute border-3 border-red-500 rounded-full w-22 h-5 pointer-events-none z-50 transition-all duration-300"
  :class="$clicks === 6
  ? 'opacity-100 scale-100 delay-0' 
  : 'opacity-0 !important scale-0 pointer-events-none delay-0'"
  style="top: 242px; left: 288px;"
>
</div>

<div 
  class="absolute border-3 border-red-500 rounded-full w-82 h-5 pointer-events-none z-50 transition-all duration-300"
  :class="$clicks === 6
  ? 'opacity-100 scale-100 delay-0' 
  : 'opacity-0 !important scale-0 pointer-events-none delay-0'"
  style="top: 364px; left: 274px;"
>
</div>

<!-- Círculo de Destaque (click-8) -->
<div 
  class="absolute border-3 border-red-500 rounded-full w-9 h-5 pointer-events-none z-50 transition-all duration-300"
  :class="$clicks === 8
  ? 'opacity-100 scale-100 delay-0' 
  : 'opacity-0 !important scale-0 pointer-events-none delay-0'"
  style="top: 269px; left: 160px;"
>
</div>

<div 
  class="absolute border-3 border-red-500 rounded-full w-14 h-5 pointer-events-none z-50 transition-all duration-300"
  :class="$clicks === 8
  ? 'opacity-100 scale-100 delay-0' 
  : 'opacity-0 !important scale-0 pointer-events-none delay-0'"
  style="top: 290px; left: 413px;"
>
</div>

<div 
  class="absolute border-3 border-red-500 rounded-full w-90 h-10 pointer-events-none z-50 transition-all duration-300"
  :class="$clicks === 8
  ? 'opacity-100 scale-100 delay-0' 
  : 'opacity-0 !important scale-0 pointer-events-none delay-0'"
  style="top: 349px; left: 247px;"
>
</div>



```cpp {all|3-10|3-10|12-21|12-21|23-32|23-32|34-40|34-40|42-43|all}{maxHeight:'400px'}
template<typename NODE>
struct SegTree {
    int N;
    vector<NODE> seg;

    SegTree(int n) : N(n), seg(4 * n) {}

    SegTree(const vector<int>& v) : N(v.size()), seg(4 * v.size()) {
        build(1, 0, N - 1, v);
    }

    void build(int no, int l, int r, const vector<int>& v) {
        if (l == r) {
            seg[no] = NODE(v[l]); 
            return;
        }
        int m = (l + r) >> 1;
        build(no << 1, l, m, v);
        build((no << 1) | 1, m + 1, r, v);
        seg[no] = NODE::merge(seg[no << 1], seg[(no << 1) | 1]);
    }

    void update(int no, int l, int r, int idx, int val) {
        if (l == r) {
            seg[no].apply(val);
            return;
        }
        int m = (l + r) >> 1;
        if (idx <= m) update(no << 1, l, m, idx, val);
        else update((no << 1) | 1, m + 1, r, idx, val);
        seg[no] = NODE::merge(seg[no << 1], seg[(no << 1) | 1]);
    }

    NODE query(int no, int l, int r, int a, int b) {
        if (b < l || r < a) return NODE();
        if (a <= l && r <= b) return seg[no];
        int m = (l + r) >> 1;
        return NODE::merge(query(no << 1, l, m, a, b),
                           query((no << 1) | 1, m + 1, r, a, b));
    }

    void update(int idx, int val) { update(1, 0, N - 1, idx, val); }
    NODE query(int l, int r) { return query(1, 0, N - 1, l, r); }
};
```
